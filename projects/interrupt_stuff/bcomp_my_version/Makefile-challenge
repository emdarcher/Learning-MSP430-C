#change to correct file
#OBJECTS = main.o 
OBJECTS = bcomp_my_version_challenge.c
ELFNAME = challenge 

CC = msp430-gcc
#change to correct ic here
CFLAGS =-Os -Wall -g -mmcu=msp430g2152
 
all : $(OBJECTS)
	$(CC) $(CFLAGS) $(OBJECTS) -o $(ELFNAME).elf
 
%.o : %.c 
	$(CC) $(CFLAGS) -c $<

 
clean:
	rm -fr $(OBJECTS) $(ELFNAME).elf
 
erase:
	mspdebug rf2500 "erase"
 
upload:
	mspdebug rf2500 "prog $(ELFNAME).elf"

tilib-upload:
	mspdebug-new tilib "prog $(ELFNAME).elf"

size:
	msp430-size $(ELFNAME).elf
